import React from 'react';

interface FeedPageProps {}

const FeedPage = ({ ...restProps }: FeedPageProps) => {
    return (
        <section className='min-h-screen pt-[100px] w-full px-4'>
            FEED PAGE
        </section>
    );
};

export default FeedPage;
